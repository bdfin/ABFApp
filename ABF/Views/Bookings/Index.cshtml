@model IEnumerable<ABF.ViewModels.EventListViewModel>

@{
    ViewBag.Title = "Index";
}

<table class="table">
    <tr>
        <th>Title</th>
        <th>Author</th>
        <th>Price</th>
        <th>Venue</th>
        <th></th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>@item.thisevent.Name</td>
        <td>@item.thisevent.Author</td>
        <td>£@item.thisevent.TicketPrice</td>
        <td>@item.thislocation.Name</td>
        <td>Some Buttons</td>
        <td>Add to Basket</td>

    </tr>
}

</table>
